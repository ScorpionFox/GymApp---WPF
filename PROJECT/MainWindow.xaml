<Window x:Class="PROJECT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PROJECT"
        xmlns:ModelWidoku="clr-namespace:PROJECT.MVVM.ModelWidoku"
        mc:Ignorable="d"
        Height="600" Width="920"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.DataContext>
        <ModelWidoku:HomeViewModel></ModelWidoku:HomeViewModel>
    </Window.DataContext>

    <Border Background="#151336" CornerRadius="15">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="669*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- Logo -->
            <TextBlock Text="Siłownia"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Foreground="LightGray"
                       FontSize="25"/>

            <!-- Przyciski menu -->
            <StackPanel Grid.Row="1">
                <RadioButton Content="Pierwsze"
                             Height="50"
                             Foreground="LightGray"
                             FontSize="16"                           
                             Style="{StaticResource MenuButtonTheme}"/>  <!-- Podpięcie stylu z pliku MenuButtonTheme-->
                <RadioButton Content="Drugie"
                             Height="50"
                             Foreground="LightGray"
                             FontSize="16"
                             Style="{StaticResource MenuButtonTheme}" Checked="RadioButton_Checked"/> <!-- Podpięcie stylu z pliku MenuButtonTheme-->
                <RadioButton Content="Trzecie"
                             Height="50"
                             Foreground="LightGray"
                             FontSize="16"
                             Style="{StaticResource MenuButtonTheme}" Checked="RadioButton_Checked_1"/> <!-- Podpięcie stylu z pliku MenuButtonTheme-->
            </StackPanel>

            <TextBox  Width="250"
                      Height="40"
                      VerticalContentAlignment="Center"
                      HorizontalAlignment="Left"
                      Margin="36,20,0,20"
                      Grid.Column="1"
                      Style="{StaticResource TextBoxMotyw}" TextChanged="TextBox_TextChanged"/>

            <ContentControl Grid.Row="1"
                            Grid.Column="1"
                            Margin="10,10,9,10"
                            Content="{Binding ObecnyWidok}"/>

        </Grid>


    </Border>
</Window>
